[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Introduction to function calling in LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pydantic",
    "torch",
    "transformers"
]